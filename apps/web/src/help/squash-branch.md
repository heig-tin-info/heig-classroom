# Branch to squash

## What it picks

The branch whose **current tree** becomes the single initial commit students
receive. Defaults to the repository's default branch; the selector only
appears when the source has several branches.

## Typical use

A teacher-canonical repository where `master` holds the working solution and
a `student` branch (or similar) holds the handout scaffold: pick the handout
branch here.
