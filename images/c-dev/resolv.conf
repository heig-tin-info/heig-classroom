# heig-codespace : reseau clos, aucun resolveur.
# Sans ces options la libc retombe sur 127.0.0.1 et attend 5 s par tentative,
# ce qui fait patienter curl, git et clangd sur chaque resolution ratee.
options timeout:1 attempts:1
